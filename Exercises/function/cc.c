#include <stdio.h>

/* int change_value(int num)
{
    num = 1337; // no return
}

int main(void)
{
    int num;

    num = 42;

    change_value(num);

    printf("%d\n", change_value(num)); // num = 42 (because no return in change_value()

    return 0;
} */

/* changevalue
void change_value(int *num);

int main(void)
{
    int num;

    num = 42;
    change_value(&num);
    printf("%d\n", num);
}

void change_value(int *foobar)
{
    *foobar = 1337;
} */